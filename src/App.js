// Goal is to make this front end and backend,
// but the backend must use Django and create two api end points

/*
 *  (Square): This is my driver function
 *  It can be exported and used by other files
 */
export default function Square() {
  // This function returns a html node
  // It is called JSX which is a combination of JavaScript and HTML
  return <button className="square">X</button>;
}
